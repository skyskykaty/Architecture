syntax = "proto3";

service AuthService {
    rpc Auth(AuthRequest) returns (AuthResponse);
}

message AuthRequest {
    string username = 1;
    string password = 2;
}

message AuthResponse {
    bool auth = 1;
}